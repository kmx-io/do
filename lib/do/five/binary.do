
module do.five.binary;

class binary;

binary :- (binary);           ~ one complement
binary (binary) :^ (binary);  ~ conjunction
binary (binary) :| (binary);  ~ disjunction
binary (binary) :& (binary);  ~ exclusion
